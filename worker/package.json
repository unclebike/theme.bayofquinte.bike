{
  "name": "rwgps-route-stats",
  "version": "1.0.0",
  "description": "Cloudflare Worker for fetching and rendering RWGPS route stats",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
